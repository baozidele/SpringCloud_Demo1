server:
  port: 80

eureka:
  client:
    register-with-eureka: false #获取服务不需要注册
    service-url:
      defaultZone: http://localhost:7001/eureka/,http://127.0.0.1:7002/eureka/

feign:
  hystrix:
    enabled: true