server:
  port: 9003

spring:
  profiles: Demo
  application:
    name: springcloud-provider-dept

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/

---
server:
  port: 9005

spring:
  profiles: DemoTest
  application:
    name: springcloud-provider-dept

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
